import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import {
  github,
  vsDark,
  nightOwl,
  dracula,
  shadesOfPurple,
} from "@code-surfer/themes";

export const theme = shadesOfPurple;

# Testing react Components

---

## Agenda

- Context
- Solutions in the community
- Proposition
  - Tools
  - Benefit of using the tools
- Proposition in practice
- Feedback

---

# Context/Background

---

<CodeSurfer theme={nightOwl}>

```js file=./code/problem.tsx title="Sample component"

```

```js 41:46 file=./code/problem.tsx title="Example of query"

```

```js 48:62 file=./code/problem.tsx title="Example of a mutation" subtitle="How do you test onError and onCompleted?"

```

```js 64:78 file=./code/problem.tsx subtitle="Trigger for the mutation"

```

```js 110:116 file=./code/problem.tsx subtitle="handles the loading state of the query"

```

```js 118:169 file=./code/problem.tsx subtitle="The entire form"

```

```js 159[17:40] file=./code/problem.tsx subtitle="Loading state when the mutation executes"

```

</CodeSurfer>

---

## Proposition (use a combination of these tools)

- [Mock Service Worker(MSW)](https://mswjs.io/)

  Mocks by intercepting requests on the network level/layer.

- [Codegen TS MSW generator](https://www.the-guild.dev/graphql/codegen/plugins/typescript/typescript-msw)

  This plugin generates mock handlers with Typescript

- [graphql-codegen-factories](https://gabinaureche.com/graphql-codegen-factories/)

  This plugin generates factories from a Graphql schema and operations

---

<CodeSurferColumns themes={[vsDark, github]}>

<Step>

```js
const magna = (aliqua) => aliqua.ut((enim, ad) => enim, 0);
```

```js
const minim = (ad, enim) => dolore.magna(ad / enim);
```

</Step>

<Step>

```js
const lorem = (ipsum, dolor, sit) => {
  const amet = dolor - ipsum;
  return consectetur.adipiscing(
    {
      elit: sed.eiusmod(sit - dolor) / amet + 2,
    },
    (tempor, incididunt) => ipsum + amet * incididunt
  );
};

const magna = (aliqua) => aliqua.ut((enim, ad) => enim, 0);
```

```js
const minim = (ad, enim) => dolore.magna(ad / enim);

const sed = (eiusmod, tempor, incididunt) => {
  const ut = tempor - eiusmod;
  return labore.et(
    {
      amet: dolore.magna(incididunt - tempor) / ut + 2,
    },
    (aliqua, elit) => eiusmod + ut * elit
  );
};
```

</Step>

</CodeSurferColumns>

---

docs:
[codesurfer.pomb.us](https://codesurfer.pomb.us)
